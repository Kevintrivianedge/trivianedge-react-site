# Wrangler _redirects only supports relative source paths.
# HTTP→HTTPS and www canonicalization must be configured in the
# Cloudflare dashboard: SSL/TLS > "Always Use HTTPS" and a Redirect Rule
# for trivianedge.com/* → https://www.trivianedge.com/:splat
